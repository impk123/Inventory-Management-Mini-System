# Redis Caching Guide

เอกสารนี้อธิบายกลไกการทำ Caching ด้วย Redis ในโปรเจกต์ Inventory Management Mini System รวมถึงโครงสร้าง Key และวิธีการทดสอบ

## 🧠 หลักการทำงาน (Caching Strategy)

ระบบใช้ Caching 2 รูปแบบหลัก เพื่อประสิทธิภาพสูงสุดและความถูกต้องของข้อมูล:

1.  **Version-based Caching (สำหรับ Lists)**
    *   ใช้สำหรับ API ที่คืนค่าเป็นรายการ (List) เช่น `/products` หรือ `/stocks`
    *   **Concept**: มีตัวแปร "Version" กลางสำหรับแต่ละ resource เมื่อมีการเพิ่ม/ลบข้อมูล เลข Version จะถูกปรับขึ้น (Increment)
    *   **Result**: Key เก่าทั้งหมดที่อิงกับ Version เดิมจะใช้ไม่ได้ทันที (Invalidate) โดยไม่ต้องตามลบทีละ Key

2.  **Direct Key IO (สำหรับ Single Item)**
    *   ใช้สำหรับ API ที่คืนค่าชิ้นเดียว เช่น `/products/:id`
    *   **Concept**: เก็บข้อมูลลง Key ตรงๆ ตาม ID ของสินค้านั้น
    *   **Result**: เมื่อมีการแก้ไขสินค้านั้นๆ จะสั่งลบ (DEL) Key นั้นทิ้งทันที

## 🔑 โครงสร้าง Key (Key Structure)

| Resource | รูปแบบ Key | TTL | ตัวอย่าง |
|----------|------------|-----|----------|
| **Product List** | `product:list:v{VERSION}:{PARAMS}` | 30 นาที | `product:list:v1:limit=20:page=1` |
| **Product Detail** | `product:id:{ID}` | 5 นาที | `product:id:1` |
| **Stock List** | `stock:list:v{VERSION}:{PARAMS}` | 30 นาที | `stock:list:v5:limit=20` |
| **Stock History** | `stock:history:v{VERSION}:id:{ID}:{PARAMS}` | 30 นาที | `stock:history:v5:id:1:limit=20` |
| **Global Versions** | `version:product`, `version:stock` | ไม่มีวันหมดอายุ | `version:product` = 2 |

## 🔄 Cache Invalidation Flow (เมื่อไหร่ Cache จะถูกลบ?)

### 1. เมื่อสร้างสินค้าใหม่ (`POST /products`)
*   **Action**: `INCR version:product`
*   **Effect**: Cache รายการสินค้า (`/products`) ทั้งหมดจะถูกรีเซ็ต

### 2. เมื่อแก้ไข/ลบสินค้า (`PUT/DELETE /products/:id`)
*   **Action 1**: `INCR version:product` (รีเซ็ตรายการสินค้า)
*   **Action 2**: `DEL product:id:{id}` (ลบ Cache info ของสินค้านั้น)

### 3. เมื่อมีการเคลื่อนไหวสต็อก (`POST /stocks`)
*   **Action 1**: `INCR version:stock` (รีเซ็ตหน้ารวมสต็อกและประวัติทั้งหมด)
*   **Action 2**: `INCR version:product` (รีเซ็ตรายการสินค้า เพราะจำนวนคงเหลือเปลี่ยน)
*   **Action 3**: `DEL product:id:{id}` (รีเซ็ต info สินค้า เพราะจำนวนคงเหลือเปลี่ยน)

## 🧪 วิธีการทดสอบ (Testing Guides)

คุณสามารถตรวจสอบข้อมูลใน Redis ได้โดยตรงผ่าน Docker และ Redis CLI

### 1. เข้าสู่ Redis CLI
```bash
docker-compose exec redis redis-cli
```

### 2. คำสั่งพื้นฐานที่ใช้บ่อย

*   **ดู Keys ทั้งหมด**:
    ```bash
    KEYS *
    ```
*   **ดูข้อมูลใน Key (JSON)**:
    ```bash
    GET product:id:1
    ```
*   **ดูเลข Version ปัจจุบัน**:
    ```bash
    GET version:product
    GET version:stock
    ```
*   **ลบข้อมูลทั้งหมด (Manual Clear)**:
    ```bash
    FLUSHALL
    ```
