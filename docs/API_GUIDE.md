# API Documentation

โปรเจกต์นี้ใช้ **Swagger (OpenAPI)** ในการทำเอกสาร API ซึ่งช่วยให้สามารถดูรายการ API ทั้งหมดและทดลองยิง Request ได้จริง

## 🔗 เข้าใช้งาน Swagger UI

เมื่อรัน Server แล้ว สามารถเข้าดูได้ที่:

> **[http://localhost:8080/swagger/index.html](http://localhost:8080/swagger/index.html)**

---

## 📋 สรุปรายการ API (API Summary)

### 🔐 Authentication (`/auth`)
*   `POST /auth/register`: ลงทะเบียนผู้ใช้ใหม่
*   `POST /auth/login`: เข้าสู่ระบบ (รับ JWT Token)
*   `GET /auth/google/login`: เข้าสู่ระบบด้วย Google
*   `GET /auth/google/callback`: Callback URL ของ Google OAuth

### 📦 Products (`/products`)
*(ต้องแนบ JWT Token)*
*   `GET /products`: ดึงรายการสินค้าทั้งหมด (รองรับ Search, Page)
*   `GET /products/:id`: ดึงรายละเอียดสินค้าตาม ID
*   `POST /products`: สร้างสินค้าใหม่
*   `PUT /products/:id`: แก้ไขข้อมูลสินค้า
*   `DELETE /products/:id`: ลบสินค้า

### 📊 Stocks (`/stocks`)
*(ต้องแนบ JWT Token)*
*   `GET /stocks`: ดูรายการสต็อกและสถานะสินค้าใกล้หมด (Low Stock)
*   `POST /stocks`: ทำรายการปรับสต็อก (IN/OUT/ADJUST)
*   `GET /stocks/product/:id`: ดูประวัติสต็อกของสินค้าชิ้นนั้น (History)

---

## ⚡ Caching Note (หมายเหตุเรื่อง Cache)

API ส่วนของการดึงข้อมูล (`GET`) มีการทำ **Caching** เพื่อความเร็ว
*   ข้อมูลอาจจะมีความล่าช้า (Delay) สูงสุด **5-30 นาที** ในกรณีที่มี Cache ค้างอยู่
*   อย่างไรก็ตาม ระบบมีกลไก **Auto Invalidate** หากมีการแก้ไขข้อมูล ระบบจะลบ Cache เก่าทิ้งให้ทันที
*   อ่านรายละเอียดเพิ่มเติมได้ที่: **[Redis Caching Guide](REDIS_GUIDE.md)**

---

## 🛠️ วิธีอัปเดต Swagger Docs

หากมีการแก้ไข Code ส่วน Comment ของ Handler ให้รันคำสั่งต่อไปนี้เพื่ออัปเดตไฟล์ Swagger:

```bash
# ติดตั้ง swag cli (ถ้ายังไม่มี)
go install github.com/swaggo/swag/cmd/swag@latest

# รันคำสั่ง generate ใน root project
swag init -g cmd/main.go
```
